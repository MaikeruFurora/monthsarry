Drop your background song here as:
  song.mp3

That's it — the floating "Play our song" button picks it up automatically.
Browsers block autoplay-with-sound, so Cheloja just has to tap the button once.

Tip: keep it under ~4 MB for fast loading on mobile.
